<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ═══════════════════ SEED BRAND COLORS ═══════════════════ -->
    <Color x:Key="SeedBackground">#0B0E13</Color>
    <Color x:Key="SeedSurface">#1C1F26</Color>
    <Color x:Key="SeedSurfaceElevated">#252830</Color>
    <Color x:Key="SeedPrimary">#00F6A1</Color>
    <Color x:Key="SeedWarning">#FF3864</Color>
    <Color x:Key="SeedInfo">#3B82F6</Color>
    <Color x:Key="SeedCaution">#F59E0B</Color>
    <Color x:Key="SeedTextPrimary">#E2E8F0</Color>
    <Color x:Key="SeedTextSecondary">#94A3B8</Color>

    <SolidColorBrush x:Key="SeedBackgroundBrush" Color="{StaticResource SeedBackground}" />
    <SolidColorBrush x:Key="SeedSurfaceBrush" Color="{StaticResource SeedSurface}" />
    <SolidColorBrush x:Key="SeedSurfaceElevatedBrush" Color="{StaticResource SeedSurfaceElevated}" />
    <SolidColorBrush x:Key="SeedPrimaryBrush" Color="{StaticResource SeedPrimary}" />
    <SolidColorBrush x:Key="SeedWarningBrush" Color="{StaticResource SeedWarning}" />
    <SolidColorBrush x:Key="SeedInfoBrush" Color="{StaticResource SeedInfo}" />
    <SolidColorBrush x:Key="SeedCautionBrush" Color="{StaticResource SeedCaution}" />
    <SolidColorBrush x:Key="SeedTextPrimaryBrush" Color="{StaticResource SeedTextPrimary}" />
    <SolidColorBrush x:Key="SeedTextSecondaryBrush" Color="{StaticResource SeedTextSecondary}" />
    <SolidColorBrush x:Key="SeedProfitBrush" Color="#00F6A1" />
    <SolidColorBrush x:Key="SeedLossBrush" Color="#FF3864" />
    <SolidColorBrush x:Key="SeedFlatBrush" Color="#94A3B8" />

    <!-- ═══════════════════ MATERIALDESIGN OVERRIDES ═══════════════════ -->
    <SolidColorBrush x:Key="MaterialDesign.Brush.Background" Color="{StaticResource SeedBackground}" />
    <SolidColorBrush x:Key="MaterialDesign.Brush.Foreground" Color="{StaticResource SeedTextPrimary}" />
    <SolidColorBrush x:Key="MaterialDesign.Brush.Card.Background" Color="{StaticResource SeedSurface}" />
    <SolidColorBrush x:Key="MaterialDesign.Brush.TextBox.HoverBackground" Color="{StaticResource SeedSurfaceElevated}" />

    <!-- ═══════════════════ GLOBAL WINDOW STYLE ═══════════════════ -->
    <Style TargetType="Window" BasedOn="{StaticResource {x:Type Window}}">
        <Setter Property="Background" Value="{StaticResource SeedBackgroundBrush}" />
        <Setter Property="Foreground" Value="{StaticResource SeedTextPrimaryBrush}" />
        <Setter Property="FontFamily" Value="Segoe UI" />
    </Style>

    <!-- ═══════════════════ NAV BUTTON ═══════════════════ -->
    <Style x:Key="NavButton" TargetType="RadioButton">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Border x:Name="Root" Background="Transparent" Padding="4,12" Cursor="Hand"
                            BorderThickness="3,0,0,0" BorderBrush="Transparent">
                        <StackPanel HorizontalAlignment="Center">
                            <ContentPresenter HorizontalAlignment="Center" />
                            <TextBlock Text="{TemplateBinding Tag}" FontSize="10" HorizontalAlignment="Center"
                                       Foreground="{StaticResource SeedTextSecondaryBrush}" Margin="0,4,0,0" />
                        </StackPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource SeedPrimaryBrush}" />
                            <Setter TargetName="Root" Property="Background">
                                <Setter.Value>
                                    <SolidColorBrush Color="{StaticResource SeedPrimary}" Opacity="0.08" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background">
                                <Setter.Value>
                                    <SolidColorBrush Color="{StaticResource SeedTextPrimary}" Opacity="0.05" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ═══════════════════ SESSION CHIP ═══════════════════ -->
    <Style x:Key="SessionChip" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource SeedSurfaceElevatedBrush}" />
        <Setter Property="CornerRadius" Value="14" />
        <Setter Property="Padding" Value="12,5" />
        <Setter Property="Margin" Value="4,0" />
    </Style>

    <!-- ═══════════════════ CARD PANEL ═══════════════════ -->
    <Style x:Key="CardPanel" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource SeedSurfaceBrush}" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="Padding" Value="16,14" />
        <Setter Property="Margin" Value="6" />
    </Style>

    <!-- ═══════════════════ TYPOGRAPHY ═══════════════════ -->
    <Style x:Key="HeroMetric" TargetType="TextBlock">
        <Setter Property="FontSize" Value="26" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource SeedTextPrimaryBrush}" />
    </Style>

    <Style x:Key="SectionHeader" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource SeedTextPrimaryBrush}" />
        <Setter Property="Margin" Value="0,12,0,8" />
    </Style>

    <Style x:Key="MetricLabel" TargetType="TextBlock">
        <Setter Property="FontSize" Value="11" />
        <Setter Property="Foreground" Value="{StaticResource SeedTextSecondaryBrush}" />
        <Setter Property="Margin" Value="0,1" />
    </Style>

    <Style x:Key="MetricValue" TargetType="TextBlock">
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="Foreground" Value="{StaticResource SeedTextPrimaryBrush}" />
    </Style>

    <!-- ═══════════════════ ACTION BUTTONS ═══════════════════ -->
    <Style x:Key="ActionCardButton" TargetType="Button">
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Padding" Value="14,12" />
        <Setter Property="Margin" Value="0,3" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" Background="{StaticResource SeedSurfaceElevatedBrush}"
                            CornerRadius="6" Padding="{TemplateBinding Padding}"
                            BorderBrush="Transparent" BorderThickness="1">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource SeedPrimaryBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bd" Property="Background">
                                <Setter.Value><SolidColorBrush Color="{StaticResource SeedPrimary}" Opacity="0.12" /></Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.35" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="PrimaryButton" TargetType="Button">
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" Background="{StaticResource SeedPrimaryBrush}"
                            CornerRadius="6" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.85" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.35" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DangerButton" TargetType="Button">
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" Background="{StaticResource SeedSurfaceElevatedBrush}"
                            CornerRadius="6" Padding="{TemplateBinding Padding}"
                            BorderBrush="{StaticResource SeedWarningBrush}" BorderThickness="1">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background">
                                <Setter.Value><SolidColorBrush Color="{StaticResource SeedWarning}" Opacity="0.15" /></Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.35" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ═══════════════════ DATAGRID ═══════════════════ -->
    <Style x:Key="SeedDataGrid" TargetType="DataGrid">
        <Setter Property="AutoGenerateColumns" Value="False" />
        <Setter Property="IsReadOnly" Value="True" />
        <Setter Property="CanUserAddRows" Value="False" />
        <Setter Property="CanUserDeleteRows" Value="False" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource SeedTextPrimaryBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="GridLinesVisibility" Value="None" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="HeadersVisibility" Value="Column" />
        <Setter Property="RowBackground" Value="Transparent" />
        <Setter Property="AlternatingRowBackground">
            <Setter.Value><SolidColorBrush Color="{StaticResource SeedSurfaceElevated}" Opacity="0.5" /></Setter.Value>
        </Setter>
        <Setter Property="HorizontalScrollBarVisibility" Value="Auto" />
        <Setter Property="VirtualizingStackPanel.IsVirtualizing" Value="True" />
        <Setter Property="VirtualizingStackPanel.VirtualizationMode" Value="Recycling" />
    </Style>

    <Style x:Key="SeedDataGridColumnHeader" TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{StaticResource SeedSurfaceElevatedBrush}" />
        <Setter Property="Foreground" Value="{StaticResource SeedTextSecondaryBrush}" />
        <Setter Property="Padding" Value="8,6" />
        <Setter Property="FontSize" Value="11" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />
        <Setter Property="BorderBrush" Value="{StaticResource SeedSurfaceElevatedBrush}" />
    </Style>

    <Style x:Key="SeedDataGridCell" TargetType="DataGridCell">
        <Setter Property="Padding" Value="8,4" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Foreground" Value="{StaticResource SeedTextPrimaryBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridCell">
                    <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                        <ContentPresenter VerticalAlignment="Center" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ═══════════════════ COMBOBOX ═══════════════════ -->
    <Style x:Key="SeedComboBox" TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
        <Setter Property="Background" Value="{StaticResource SeedSurfaceElevatedBrush}" />
        <Setter Property="Foreground" Value="{StaticResource SeedTextPrimaryBrush}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Padding" Value="10,7" />
    </Style>

</ResourceDictionary>
